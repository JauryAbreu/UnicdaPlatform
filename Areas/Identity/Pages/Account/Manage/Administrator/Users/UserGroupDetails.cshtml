@page
@model UnicdaPlatform.Areas.Identity.Pages.Account.Manage.Administrator.Users.UserGroupDetailsModel
@{
    ViewBag.Title = "Customer Details";
    Layout = "~/Areas/Identity/Pages/Account/Manage/Administrator/_AdmLayout.cshtml";
}

<div class="container-fluid">
    <div class="cont-all">
        <div class="d-sm-flex align-items-center justify-content-between mb-4">
            <h1 class="tit-general">GRUPO DE USUARIO</h1>
        </div>
        <div class="row">
            <div id="disponibles" class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
                <div class="card shadow mb-4">
                    <div class="card-header py-3">
                        <div class="d-sm-flex align-items-center justify-content-between">
                            <h6 class="m-0 font-weight-bold text-black">Grupo de Usuarios Registrados</h6>
                            <a asp-area="Identity" asp-page="/Account/Manage/Administrator/Users/UserGroupInformation"
                               class="d-none d-sm-inline-block btn btn-sm btn-blue shadow-sm">
                            <i class="fas fa-plus fa-sm text-white"></i> Ingresar Grupo de Usuario</a>
                        </div>

                    </div>
                    <div class="card-body">
                        <div class="table-responsive tabla-disponible">
                            @using (Html.BeginForm())
                            {
                                <table class="table" id="dataTable" width="100%" cellspacing="0">
                                    <thead>
                                        <tr>

                                            <th>Codigo</th>
                                            <th>Descripcion</th>
                                            <th></th>

                                        </tr>
                                    </thead>
                                    <tbody>
                                        @if (Model.userGroups.Count > 0)
                                        {
                                            @foreach (var term in Model.userGroups)
                                            {
                                                <tr style="@(term.Deleted == true ? "background-color:#FAE6E9" : "")">
                                                    <td>@Html.DisplayFor(IndexModel => term.Id)</td>
                                                    <td>@Html.DisplayFor(IndexModel => term.Name)</td>
                                                    <td>
                                                        <div class="row btn-accion">
                                                            <div class="dropdown no-arrow">
                                                                <a class="dropdown-toggle" href="#" role="button" id="dropdownMenuLink"
                                                                   data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                                                    <i class="fas fa-ellipsis-v fa-md fa-fw text-black"></i>
                                                                </a>
                                                                <div class="dropdown-menu dropdown-menu-right shadow animated--fade-in"
                                                                     aria-labelledby="dropdownMenuLink">
                                                                    <div class="dropdown-header">Otras opciones</div>
                                                                    <button value='@term.Id' type="submit" name="view" class="dropdown-item">
                                                                        Ver Detalles
                                                                    </button>
                                                                    <button value='@term.Id' type="submit" name="groupId" class="dropdown-item">
                                                                        Asignar Permisos
                                                                    </button>
                                                                    <div class="form-group">
                                                                        <button value='@term.Id' type="submit" name="delete" class="dropdown-item">
                                                                            Eliminar
                                                                        </button>
                                                                    </div>
                                                                </div>
                                                            </div>
                                                        </div>
                                                </td>
                                                </tr>
                                            }
                                        }
                                        </tbody>
                                        </table>
                                    }
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>